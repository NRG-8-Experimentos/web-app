export interface MemberStatistics {
  completed: number;
  done: number;
  inProgress: number;
  pending: number;
  overdue: number;
  rescheduled: number;
  notRescheduled: number;
  avgCompletionDays: number;
}
